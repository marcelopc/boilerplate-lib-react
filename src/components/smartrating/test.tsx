import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/vitest';
import SmartRating from './SmartRating';

it('Quando renderizar o componente, deve conter o texto "Hello World"', () => {
    expect(1).toBe(1);
});
